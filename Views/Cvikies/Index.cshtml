@model IEnumerable<WebApplication4.Entities.Cviky>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DruhCviku)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CasCviku)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DenCviku)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DruhCviku)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CasCviku)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DenCviku)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CvikyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CvikyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CvikyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
